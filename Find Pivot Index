class Solution:
    def pivotIndex(self, nums: List[int]) -> int:
        right_sum = sum(nums)
        left_sum, i = 0, 0
        for ij in nums:
            right_sum -= ij
            if right_sum==left_sum:
                return i
            i+=1
            left_sum+=ij
        return -1
        
        
